export type Nearby = {
  id: string;
  img: string;
  location: string;
  distance: string;
};

export type MediumCardType = {
  img: string;
  title: string;
};

export type InfoCardType = {
  id:string,
  lat:string,
  long:string,
  img: string;
  location: string;
  title: string;
  description: string;
  star: string;
  price: string;
  total: string;
};
